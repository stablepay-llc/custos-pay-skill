# OpenAI / OpenAI-compatible (Codex CLI, Continue, Cline, etc.)
# Append to your shell rc.

export OPENAI_BASE_URL=""
export OPENAI_API_KEY=""

# Optional — pin a model name the gateway exposes:
# export OPENAI_MODEL=""
